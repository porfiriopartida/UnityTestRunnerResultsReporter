<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net5.0</TargetFramework>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="Mono.Options" Version="5.3.0.1" />
    <PackageReference Include="Newtonsoft.Json" Version="11.0.2" />
  </ItemGroup>

  <PropertyGroup>
    <PostBuildEvent Condition="cp $(SolutionDir)/Template $(TargetDir)/Template" />
    <PostBuildEvent Condition="echo $(SolutionDir) " />
    <PostBuildEvent Condition="echo $(OutDir) " />
<!--    <Exec Command="cp $(SolutionDir)/Template; $(TargetDir)/Template; /e /Y" />-->
<!--    <Copy SourceFiles="Template" DestinationFolder="$(OutDir)/Template" />-->
  </PropertyGroup>

</Project>
